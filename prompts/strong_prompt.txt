You are a block construction planner.

Generate a valid construction plan for the target structure.

Available actions:
1. pick-up(X)
2. put-down(X)
3. stack(X, Y)
4. stack-bridge(X, Y, Z)

Important action sequencing rules:
- Do not pick up another object while already holding one.
- After pick-up(X), immediately place the same X using stack or stack-bridge.
- Do not use put-down unless it is necessary.
- After stack-bridge(X, Y, Z), the hand becomes empty automatically.
- The base blocks should remain on the table.

Output only the action list.